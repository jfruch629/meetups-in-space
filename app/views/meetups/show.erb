<h1><%= @meetup.name %></h1>

<h4><%= @meetup.description %></h4>
<p>Location: <%= @meetup.location %></p>
<p>This meetup has been created by: <p> <%= @meetup.creator %>

  <img src="<%= @meetup.users[0].avatar_url %>" width="100" height="100">
</p>

<br/><br/><br/>

<div class="joined_list">
<h3> Here's who is going already! </h3>
<ul>
  <% @meetup.users[1..-1].each do |user| %>
    <img src="<%= user.avatar_url %>" width="100" height="100">
    <li><%= user.username %></li>
  <% end %>
</ul>

  <form action = "/meetups/<%=@meetup.id%>" method = "post">
      <input type="submit" name="add_user" value="Join Meetup">
  </form>
